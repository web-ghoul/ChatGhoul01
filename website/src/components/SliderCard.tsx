
const SliderCard = ({ index }: { index: number }) => {
    return (
        <div class="bg-white h-[300px] min-w-[300px] rounded-xl flex items-center justify-center font-bold text-black">
            Hello {index}
        </div>
    )
}

export default SliderCard
